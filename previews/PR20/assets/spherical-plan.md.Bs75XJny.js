import{_ as a,o as t,c as i,ao as o}from"./chunks/framework.iBCT5tmE.js";const h=JSON.parse('{"title":"Spherical geometry plan","description":"","frontmatter":{},"headers":[],"relativePath":"spherical-plan.md","filePath":"spherical-plan.md","lastUpdated":null}'),n={name:"spherical-plan.md"};function r(l,e,s,d,p,c){return t(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="Spherical-geometry-plan" tabindex="-1">Spherical geometry plan <a class="header-anchor" href="#Spherical-geometry-plan" aria-label="Permalink to &quot;Spherical geometry plan {#Spherical-geometry-plan}&quot;">​</a></h1><p>VectorDataCubes uses a lookup&#39;s manifold consistently for its spatial index and exact predicates. Public spherical geometries use longitude/latitude; unit-sphere XYZ bounds are internal accelerators.</p><h2 id="Agreed-design" tabindex="-1">Agreed design <a class="header-anchor" href="#Agreed-design" aria-label="Permalink to &quot;Agreed design {#Agreed-design}&quot;">​</a></h2><ul><li><p>Expose the existing typed <code>GeometryLookup.manifold</code>. Planar remains the default; spherical input uses longitude/latitude in degrees and great-circle edges.</p></li><li><p>Keep lazy R-trees: planar lookups index XY bounds, spherical lookups index conservative XYZ bounds from <code>GeometryOps.extent</code>. Rebuild the cache when data or manifold changes. Public dimension bounds remain longitude/latitude.</p></li><li><p>Use manifold-aware predicates, including RelateNG for spherical <code>crosses</code> and <code>overlaps</code>. Keep <code>At(geometry)</code> coordinate equality, boundary-inclusive <code>Contains</code>, sorted selections, and lowest-index nearest ties.</p></li><li><p>Interpret finite interval boxes as polygons with great-circle edges in the first cut. Coordinate windows with constant-latitude boundaries and cross-space operations are later work. Reject unbounded or ambiguous spherical boxes.</p></li><li><p>Follow <a href="https://geoparquet.org/releases/v1.1.0/" target="_blank" rel="noreferrer">GeoParquet&#39;s column metadata semantics</a>: <code>edges</code> is <code>planar</code> or <code>spherical</code>; <code>orientation</code>, when asserted, is <code>counterclockwise</code>. Missing <code>edges</code> means planar. Preserve these per geometry column through DataAPI column metadata.</p></li><li><p>Continue emitting the existing DataAPI table keys <code>GEOINTERFACE:crs</code> and <code>GEOINTERFACE:geometrycolumns</code>. Datum and sphere/ellipsoid parameters belong to the CRS. Edge and ring-interior interpretation are separate geometry semantics.</p></li><li><p>Emit longitude/latitude geometry, never internal unit-sphere coordinates under a geographic CRS. File writers remain responsible for mapping DataAPI metadata into their supported formats; this package does not become a GeoParquet writer.</p></li><li><p>Normalize <code>UnitSphericalPoint</code> input and queries back to public longitude/latitude coordinates at the lookup boundary.</p></li></ul><h2 id="Implementation-layers" tabindex="-1">Implementation layers <a class="header-anchor" href="#Implementation-layers" aria-label="Permalink to &quot;Implementation layers {#Implementation-layers}&quot;">​</a></h2><ol><li><p>Add explicit manifold construction and preservation, XY/XYZ lazy indexing, manifold-aware predicates, and finite geodesic interval selection.</p></li><li><p>Implement spherical nearest-point selection with an exhaustive scan, then accelerate it using XYZ chord bounds converted to angular distance. General spherical point-to-line/polygon distance remains a follow-up.</p></li><li><p>Round-trip edge/orientation semantics through table column metadata. Preserve the supplied CRS unchanged. With Proj loaded, a spherical CRS keeps its declared radius exactly; an ellipsoidal datum uses the arithmetic mean radius <code>(2a+b)/3</code>. A custom radius must agree with the CRS, and without a CRS only the GeometryOps default radius can round-trip.</p></li><li><p>Preserve the manifold through point extraction. Reject spherical zonal burning and reprojection until their space-conversion semantics are implemented.</p></li></ol><p>The implementation uses released GeometryOps APIs and explicit manifold bounds, without depending on an unmerged tree-query API. Proj is an optional extension: GeoParquet <code>edges</code> still chooses the manifold independently, while the CRS supplies and validates its physical radius.</p><h2 id="Verification" tabindex="-1">Verification <a class="header-anchor" href="#Verification" aria-label="Permalink to &quot;Verification {#Verification}&quot;">​</a></h2><p>Compare indexed selection against exhaustive spherical predicates for antimeridian and polar geometries, boundaries, holes, crossings, overlaps, Float32 input, and geodesic boxes. Check lazy cache invalidation, slicing/view/reverse, unchanged public bounds after index construction, nearest-point ties, mixed-manifold table columns, metadata round-trips, and unchanged longitude/latitude output. Run the existing planar test suite.</p><h2 id="Follow-ups" tabindex="-1">Follow-ups <a class="header-anchor" href="#Follow-ups" aria-label="Permalink to &quot;Follow-ups {#Follow-ups}&quot;">​</a></h2><ul><li><p>General spherical point-to-line/polygon distance. Point lookup tree pruning uses an XYZ-box chord lower bound and exhaustive-scan equivalence tests.</p></li><li><p>Explicit reprojection and spherical rasterization/zonal semantics.</p></li><li><p>Format-specific metadata bridges.</p></li></ul><p>Relevant upstream work: <a href="https://github.com/evetion/GeoDataFrames.jl/pull/167" target="_blank" rel="noreferrer">GeoDataFrames #167</a>, <a href="https://github.com/JuliaGeo/GeometryOps.jl/pull/506" target="_blank" rel="noreferrer">GeometryOps #506</a>, and <a href="https://github.com/JuliaGeo/GeometryOps.jl/pull/504" target="_blank" rel="noreferrer">GeometryOps #504</a>.</p>',12)])])}const m=a(n,[["render",r]]);export{h as __pageData,m as default};
